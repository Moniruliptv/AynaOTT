name: 🔁 AynaOTT M3U to JSON Auto Update

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"   # প্রতি ৬ ঘন্টা পর পর রান

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: 📥 Install Dependencies
        run: |
          pip install requests

      - name: ▶️ Run M3U Parser Script
        run: |
          python script.py

      - name: 💾 Commit & Push Changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add playlist.json count.json
          git commit -m "🔄 Auto update playlist & count" || echo "No changes"
          git push
